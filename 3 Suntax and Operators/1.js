function solve(arr1, arr2, arr3) {
    let all ;
    let avverage ;

    all = arr1.length + arr2.length + arr3.length;
    avverage = Math.floor(all/3);

    console.log(all);
    console.log(avverage);
}


solve('aaa', 'aa', 'a');